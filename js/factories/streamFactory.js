/**
 * Created by lschubert on 10/24/15.
 * used for the making of posts on the stream and the retrieving of stream content
 */
app.factory('stream', streamFactory);

streamFactory.$inject = ['$http'];

streamFactory = function($http){
    var stream  = {};

    //function to get all

    //function to create new

    //function to get post by id
    return stream;
}